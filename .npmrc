# Temporary use of legacy peer deps due to dependency conflicts
# TODO: Remove this once all packages are updated to compatible versions
legacy-peer-deps=true

# Registry configuration
registry=https://registry.npmjs.org/
